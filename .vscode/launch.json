{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Open browser to localhost:3000",
        "type": "pwa-chrome",
        "request": "launch",
        "url": "http://localhost:3000",
        "webRoot": "${workspaceFolder}"
      }
    ]
  }
  